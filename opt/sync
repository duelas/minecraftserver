#!/usr/bin/env bash
HOST=22ftplink.ddns.net
USER=guest
PASS=guest



  if [ -d world ]; then  
    cd world
sudo ftp -inv $HOST << EOF
user $USER $PASS
mput *
bye
EOF
  else
    mkdir -p world
    cd world
sudo wget -nH --cut-dirs=1 ftp://$HOST/* --user=$USER --password=$PASS -r
  fi
