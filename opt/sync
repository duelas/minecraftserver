#!/usr/bin bash
HOST=22ftplink.ddns.net
USER=guest
PASS=guest



  if [ -d world ]; then  
    cd world
ftp -inv $HOST << EOF
user $USER $PASS
mput *
bye
EOF
  else
    mkdir -p world
    cd world
ftp -inv $HOST << EOF
user $USER $PASS
mget *
bye
EOF
  fi
