#!/bin/sh
HOST='22ftplink.ddns.net'
USER='guest'
PASS='guest'

  if [ -d world ]; then  
    cd world
ftp -inv $HOST << EOF
quote USER $USER
quote PASS $PASS
mput *
quit
EOF
  else
    mkdir -p world
    cd world
ftp -inv $HOST << EOF
quote USER $USER
quote PASS $PASS
mget *
quit
EOF
  fi
